{
  "name": "thermal-heatmap",
  "version": "0.1.0",
  "private": true,
  "description": "Shared root for Prisma CLI (schema + migrations live here; web/ and thermal-worker/ are the deployable packages)",
  "scripts": {
    "postinstall": "prisma generate"
  },
  "devDependencies": {
    "dotenv": "^17.4.0",
    "prisma": "^7.6.0"
  }
}
